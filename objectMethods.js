
const obj = {
    name: "Yukti",
    age: 23,
    city: "Banglore"
};

const entries = Object.entries(obj);
console.log(entries);

const keys = Object.keys(obj);
console.log(keys);

const values= Object.values(obj);
console.log(values);

